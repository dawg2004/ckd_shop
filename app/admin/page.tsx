'use client';import Link from 'next/link';import { useStore } from '@/components/store';
export default function A(){const {products}=useStore();return <main className='container-x py-8'><h1 className='text-2xl mb-4'>管理ダッシュボード</h1><div className='grid md:grid-cols-5 gap-3 mb-6'>{['本日の注文数 3','今月売上 ¥128,000','在庫切れ '+products.filter((p:any)=>p.stock_quantity===0).length,'ギフト注文数 12','公開商品 '+products.filter((p:any)=>p.is_published).length].map(x=><div key={x} className='card'>{x}</div>)}</div><div className='flex gap-3'>{['products','categories','orders','settings'].map(x=><Link className='btn' key={x} href={`/admin/${x}`}>{x}</Link>)}</div></main>}
